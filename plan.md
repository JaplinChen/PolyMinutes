# PolyMinutes 架構計劃

會議室多語自動翻譯。即時字幕投影到電視 + 會後多語對照逐字稿。

## 產品定義

| 項目 | 決定 |
|---|---|
| 語言 | 可設定 2 或 3 種。自動判斷來源語言，翻成其餘語言。預設繁體中文（台灣）+ 越南語 + 英語 |
| 中文變體 | 一律繁體中文（台灣用語）。Whisper 預設輸出簡體，必須轉換 |
| 使用者 | 單機單人，無登入、無權限、無多用戶 |
| 顯示 | 原文 + 各語言譯文同列。排版格式可設定 |
| 主要場景 | Microsoft Teams 線上會議（90%），但不綁定 Teams |
| 執行平台 | Windows 筆電或 Mac Mini，同一套程式 |
| 執行方式 | 一台專用電腦以旁聽身分加入會議：不外放、不開麥克風，只收音、翻譯、顯示 |
| 即時輸出 | 字幕頁全螢幕投影到獨立電視，供會議室所有人觀看 |
| 即時修飾 | 字幕先秒出暫定版，後續語句到齊後由 LLM 就地改寫該行 |
| 會後輸出 | 依發言者分段的多語對照逐字稿 + 摘要 |
| 隱私 | 音訊與逐字稿留在本機；只有翻譯呼叫雲端 |

字幕投影到電視這件事，順帶解掉了先前「中譯越那半邊給誰看」的疑問：電視是會議室的共用畫面，室內不同母語的人各自讀自己看得懂的那一行，兩個方向都有觀眾。遠端與會者仍看不到電視，若他們也需要字幕，見「待確認事項」。

## 架構決策紀錄

計劃前後推翻過數次，每次的理由留在這裡，避免之後重新爭論。

**1. 不走 Microsoft Graph API**
確認拿不到 `OnlineMeetingTranscript.Read.All`。會後逐字稿改由本機錄音重跑產生，完全不碰 Teams API——附帶好處是任何會議軟體都適用。

**2. 單通道旁聽，來源語言靠語者主導語言判定**

一度設計為雙通道：loopback = 對方、麥克風 = 本人，理由是雙向翻譯必須知道發言者才能決定方向，而混流後只能靠語言偵測反推，短句（人名、數字、「OK」）必然誤判。

確認這台電腦是純旁聽——不外放、不開麥克風——後，雙通道不成立：所有與會者都從同一條音訊進來。附帶好處是回音問題完全消失。

來源語言改由語者身分決定，而不是逐句語言偵測：語者分離已經知道「這句是 Speaker 2」，而 Speaker 2 累積下來講的都是越南語，所以短句就算偵測不準，也能用該語者的主導語言判定。第一次遇到某個語者時才用自動偵測，之後以其主導語言為準；偵測結果與主導語言衝突時，只有連續數句都衝突才切換（避免一句誤判就整個翻錯）。

這比原本的雙通道更弱一點，但比「只靠逐句偵測」穩得多，且不需要任何額外機制——語者分離本來就要做。

**3. 跨平台音訊：不用 WASAPI，改用虛擬音效裝置 + PortAudio**

原本規劃 WASAPI loopback，那是 Windows 專有 API，Mac Mini 用不了。

但兩個平台本來就都需要虛擬音效裝置：Windows 上「不外放」不能用系統靜音達成（靜音後 loopback 錄到無聲），必須把輸出導到沒接喇叭的裝置；macOS 根本沒有內建的系統音回錄，一定要裝 BlackHole 之類的東西。

既然兩邊都要裝，錄音就退化成「從某個輸入裝置收音」這件最普通的事——用 PortAudio（Python 的 `sounddevice`）兩個平台同一份程式碼，完全不需要平台專屬分支。原本要寫的 WASAPI 程式碼直接消失。

代價是部署時要在機器上裝一個虛擬音效裝置並把 Teams 輸出指過去。這一步無論如何都躲不掉，寫進安裝說明。

**4. 語言數從「固定中越雙向」推廣為「可設定 2 至 3 種」**

原設計寫死中越互譯。改為可設定的語言集合後，既有機制直接沿用不必新增：語者主導語言從「二選一」變成「N 選一」，翻譯從「翻成另一種」變成「翻成集合中其餘各種」。

三語言時每句要產 2 份譯文，但併在同一次 API 呼叫裡回傳，呼叫次數不變。

真正變難的是語言偵測：三個候選比兩個容易錯。靠語者主導語言仍能擋掉大部分，但首次遇到新語者的那幾句風險較高，列入風險表。

**5. 繁體中文與中英夾雜，是必須專門處理的兩件事**

台灣的中文使用情境有兩個特性會直接打壞預設行為：

*Whisper 的中文輸出是簡體*，即使輸入是台灣國語。字幕投到電視上跳簡體字是很顯眼的失敗。處理方式：ASR 輸出後接 OpenCC 做簡轉繁（`s2twp` 設定檔，會一併轉換台灣慣用詞，例如「軟件」→「軟體」）；LLM 翻譯的提示詞也要明確要求輸出台灣繁體中文，不能只寫「中文」。

*中英夾雜*，例如「這個 schedule 要 delay 一週」。這與「英語是設定語言之一」直接衝突——程式可能誤判該語者改講英語，把他的主導語言翻掉。

處理方式分兩層：主導語言的切換門檻對「中文 ↔ 英語」這組要明顯提高（其他語言組合維持原本的連續數句即可），並允許在設定中把某個語者的語言釘死不再自動切換。

夾雜的英文詞怎麼翻是另一個問題：`schedule`、`delay` 這類詞在跨國團隊裡往往就是共通詞彙，翻成越南文反而更難懂。因此術語表需要一個「保留原文不翻譯」的選項，而不只是「A 翻成 B」。這是中英夾雜帶出的新需求。

口音方面，台灣國語（含台語借詞）在 `small` 模型下辨識率會再降。除了會後用 `large-v3` 重跑之外，可用術語表內容組成 Whisper 的 initial prompt 來偏置常用詞彙。實際效果第二批實測。

**5. 用 sherpa-onnx 取代 faster-whisper + Resemblyzer**
原計劃要裝 faster-whisper（辨識）、Resemblyzer（語者嵌入）、webrtcvad（切句），其中 Resemblyzer 會拖進 torch，光它一個就約 2GB。

sherpa-onnx 一個函式庫涵蓋 VAD + Whisper 辨識 + 語者嵌入 + 語者分離，有 Python binding，純 CPU 可跑，體積小得多。三個模組的依賴塌縮成一個。附帶好處是 Windows 與 macOS（含 Apple Silicon）都有預編譯套件，跨平台不必額外處理。

**6. 移除登入與權限機制**

從 OpenWA-lab 複製過來的頁面帶著多租戶設計：登入頁、API 金鑰管理、admin/operator/viewer 三種角色。確認為單機單人使用後全部移除——服務只監聽本機，沒有多人場景，留著只是讓每次開啟都要輸金鑰、後端還要實作一整層權限檢查。

`canWrite` 在四個頁面裡都有引用，改為固定為真，頁面本身不必改動。

**7. 不做 Tauri 桌面應用**
一度打算照 meetily 的形狀改成 Tauri + Rust。唯一的理由是「字幕要蓋在 Teams 視窗上方，而瀏覽器做不到視窗置頂穿透」。

需求確認為「獨立字幕視窗，不需要蓋在 Teams 上」後，這個理由消失，Tauri 連同 Rust toolchain 一起砍掉。字幕改為一個獨立的網頁，開在自己的瀏覽器視窗裡，可以丟到第二個螢幕——實際上比蓋在 Teams 上更好用。

sherpa-onnx 有 Python binding，所以「不必裝 torch」這個好處完全保留，不需要為它改寫成 Rust。

最終形狀：本機 Python 服務負責錄音與處理，瀏覽器負責顯示。啟動用一個腳本把服務拉起來並開瀏覽器即可（Windows 用 `.bat`，macOS 用 `.command`）。日後若真的想要雙擊圖示的桌面感，再包一層 pywebview 之類的薄殼，架構不用動。

字幕頁在瀏覽器全螢幕（F11 / 進入全螢幕 API）後拖到電視那個顯示器，就是投影所需的全部操作——不需要為此寫任何程式。

## 系統組成

```
虛擬音效裝置（VB-Cable / BlackHole）← Teams 音訊輸出
        │
        ▼
本機 Python 服務（FastAPI）
├ audio      PortAudio 收音 + 原音落地（跨平台，無平台專屬碼）
├ asr        sherpa-onnx：VAD → Whisper（指定語言）
├ diarize    語者嵌入 → 聚類 → 語者主導語言
├ translate  翻成語言集合中其餘各種，帶上下文 + 術語表
├ refine     併入下一句請求的二次修飾，就地改寫已顯示字幕
├ keyproxy   多金鑰輪替與配額追蹤
└ store      SQLite
        │                              │
        │ REST（設定、術語、紀錄）        │ WebSocket（字幕新增 / 更新）
        ▼                              ▼
瀏覽器（React + Vite）
├ 管理視窗  /glossary・/settings/llm・keyproxy・display・/sessions
└ 字幕視窗  /live   原文 + N-1 種譯文，全螢幕投影到電視
        │
        ▼
Claude API（唯一對外呼叫）
```

## 元件分工

**audio** — 用 `sounddevice`（PortAudio）從虛擬音效裝置收音，Windows 與 macOS 同一份程式碼。原音在任何處理之前先寫入 `session_{ts}.wav`，且獨立於轉錄管線的成敗：沒有 Graph API 就沒有第二份逐字稿來源，轉錄崩了錄音不能跟著掉。

以裝置原生取樣率與聲道數擷取，再降混單聲道並用 soxr 重取樣到 16 kHz。不能直接要求 16 kHz——WASAPI 共享模式只接受裝置原生取樣率，而虛擬音效裝置多半跑 44.1 或 48 kHz。

Windows 把同一個裝置掛在 MME / DirectSound / WASAPI / WDM-KS 四個介面下，名稱一模一樣。實測發現「列得出來」不等於「開得起來」，甚至同一台機器不同時間可開的介面也不同；更糟的是 WDM-KS 會開啟成功卻完全不送資料，表現為逐字稿空白而毫無錯誤。因此依延遲排序後逐一嘗試，並要求每個候選在 1 秒內實際送出第一個音訊區塊才算通過，否則換下一個。

部署前置：把 Teams 的音訊輸出指向虛擬音效裝置（Windows 用 VB-Cable，macOS 用 BlackHole），程式再從該裝置收音。這同時達成「不外放」與「錄得到」——用系統靜音是不行的，靜音後錄到的是無聲。

會議室內發言者的聲音路徑：會議室另一台 Teams 設備的麥克風 → Teams 伺服器 → 這台旁聽電腦。也就是說室內發言經過 Teams 的壓縮與降噪處理，辨識率會比原始麥克風收音差一些。這是單機旁聽架構的固有代價，第二批實測時要各語言分開評估，不能只看整體。

**asr** — sherpa-onnx：Silero VAD 切句 → Whisper 轉錄。轉錄時**指定**語言而非讓 Whisper 自動偵測，語言由 diarize 給的語者主導語言決定。首次遇到某語者時才用自動偵測。

sherpa 的 `result.lang` 會回報它實際用哪個語言解碼，這是自動偵測時唯一能得知結果語言的管道——沒有它，語者主導語言永遠無法建立，整場會停在自動偵測。

sherpa 的 Whisper 只處理前 30 秒且靜默丟棄其餘部分。VAD 設了 20 秒上限，但實測仍會出現超長片段，因此轉錄層再加一道：超過 25 秒的片段切塊後分別解碼再合併。防護放在所有呼叫者共用的那一層，而不是信任單一設定。

強制語言若判錯不會優雅降級，而是崩塌成重複填充詞（對英語音檔強制中文會得到「前來,前來,前來…」）。這種崩塌有明顯特徵（詞元重複率極高），偵測到就改用自動偵測重跑一次，而不是把垃圾送上電視。

即時階段用 `small`，會後重跑用 `large-v3`（不受即時性限制，可用 beam search 求準）。硬體加速自動偵測：Windows 有 NVIDIA 顯卡走 CUDA，Apple Silicon 走 CoreML，都沒有就純 CPU。

中文輸出接 OpenCC `s2twp` 轉繁體並套用台灣慣用詞。這一步不可省——Whisper 給的中文一律是簡體，字幕直接投到電視上會很明顯。

術語表內容組成 initial prompt 餵給 Whisper，偏置公司與產品的專有名詞，改善台灣國語口音下的辨識率。

**diarize** — loopback 是全體與會者的混音，語者分離是唯一的區分手段，因此是本案的核心而非附屬功能：翻譯方向也依賴它。即時走線上增量聚類（新片段與已知質心比餘弦相似度，超過門檻歸入並更新質心，否則開新 ID）；會後走全域階層聚類，能看到整場所有片段，可回頭修正即時階段的誤判。短片段（< 1 秒）嵌入不穩，繼承前一段的 ID。

每個語者累積一份主導語言統計（在設定的語言集合內）。這份統計先餵給 asr 決定轉錄語言，再決定要翻成哪些語言——順序見「資料流」。

聲紋嵌入只需要聲學特徵、不需要先轉錄，所以聚類放在 ASR 之前不增加延遲。

**translate** — Claude API，帶前幾句上下文與命中的術語表項目。整句翻譯而非逐字串流，品質差距明顯。

來源語言取自該語者的主導語言，目標語言是設定集合中的其餘各種。三語言時一次呼叫同時回傳兩份譯文，不拆成兩次。

提示詞要求輸出**台灣繁體中文**，不能只寫「中文」——只寫中文模型多半給簡體或中國用語。

術語表支援三種處理：翻成指定譯詞、保留原文不翻譯、以及僅作為 ASR 提示詞不介入翻譯。「保留原文」是中英夾雜帶出的需求：`schedule`、`delay` 這類詞在跨國團隊裡是共通詞彙，硬翻成越南文反而更難懂。

**refine** — 即時修飾。字幕分兩段呈現：VAD 一切出句子就先送出暫定翻譯（求快），待後續語句到齊後，把該行連同上下文重譯，就地改寫已顯示的那一行（求準）。

修飾不另外呼叫 API，而是併入下一句的翻譯請求：一次呼叫同時「翻譯新句 + 檢查前一句是否需要修正」。API 用量因此不變，不會因為加了修飾就翻倍。回傳格式讓模型自行決定前句是否需要改，不需要改就不回傳該欄位。

每行有自己的 ID，WebSocket 送「新增」與「更新」兩種事件，字幕頁必須支援改寫已顯示的行，不能只往下追加。已改寫過的行鎖定不再變動，避免畫面持續跳動干擾閱讀。

**keyproxy** — 沿用 OpenWA-lab 的多金鑰輪替。配額頁面要顯示 audio seconds 而不只是請求數：轉錄場景下音訊秒數會先耗盡，只看請求數會誤判還有餘裕。

**display** — 字幕呈現設定，存在 store、由 `/settings/display` 編輯、`/live` 讀取套用：

| 設定 | 選項 |
|---|---|
| 語言集合 | 2 或 3 種，並指定顯示順序 |
| 原文位置 | 置頂 / 置底 / 不顯示（只看譯文） |
| 字級 | 電視觀看距離用，需明顯大於一般網頁 |
| 保留行數 | 畫面上同時顯示幾句，超過捲動 |
| 語者標示 | 顯示 Speaker 名稱與否、是否用顏色區分不同語者 |
| 主題 | 深色 / 淺色（電視多半深色較舒適） |

**store** — SQLite。術語表、會議紀錄、金鑰、設定、顯示格式。

## 資料流

**即時**

```
虛擬音效裝置 → VAD 切句 → 語者嵌入 → 聚類 → Speaker ID → 主導語言
   │                                                       │
   │                                                       ▼
   │                                       Whisper ASR（指定語言，不自動偵測）
   │                                                       │
   │                                                       ▼
   │                                    翻譯（→ 語言集合中其餘各種，一次呼叫）
   │                                                       │
   │                                      暫定字幕（秒出）──┤
   │                                             │         │
   │                                             │    下一句翻譯時
   │                                             │    順帶修飾前一句
   │                                             ▼         ▼
   │                                  字幕視窗（新增 / 就地改寫）→ 電視
   └─→ session_{ts}.wav
```

**順序不可對調**：語者必須先於辨識確定。Whisper 轉錄要指定語言，若讓它自動偵測而判錯，輸出的會是另一種語言的胡言亂語——毀掉的不只是翻譯方向，連逐字稿內容都沒了。主導語言先餵給 ASR，再決定翻譯方向。

語者嵌入只需要聲學特徵，不需要先轉錄，所以放在 ASR 之前不會增加延遲。

暫定字幕延遲估計 3-5 秒：VAD 靜音判定 0.5s + 嵌入聚類 0.1s + ASR 1-2s + 翻譯 API 1-2s。
修飾版再晚 5-10 秒（要等後續語句到齊）。

**會後**

1. `large-v3` 慢速重跑錄音
2. 全域階層聚類重跑語者分離，修正即時誤判
3. 依修正後的語者重算各自主導語言，修正語言判錯的句子（含重新轉錄）
4. 套用語者名稱對應表，把 Speaker ID 換成真名
5. 整段送 LLM 精修：修正 ASR 錯字、統一術語、補標點
6. 輸出多語對照逐字稿 + 決議摘要 + 待辦事項

語者名稱：程式認得出「這幾句是同一個人」，但不知道他叫什麼，先標 `Speaker 1/2/3`，由使用者在會議紀錄頁手動對應一次。

這台電腦既然加入了會議，Teams 視窗上就有參與者名單，理論上可用 UI Automation 讀出來當候選名字，甚至靠 Teams 的「誰正在說話」標示直接取得發言者。但這條路綁死 Teams 且會隨改版失效，先不做——列在「值得評估」而非「拿不到」。

## 目錄結構

```
dashboard/            React 前端（已建立，build 通過）
  src/pages/          Glossary, LlmSettings, KeyProxy, ApiKeys
  src/pages/Live.tsx      字幕頁，供電視全螢幕顯示（待建）
  src/pages/Display.tsx   顯示格式與語言集合設定（待建）
  src/components/     PageHeader, Toast, EditableKeyValueTable, llm/*
server/
  main.py             FastAPI 進入點（已建立）+ WebSocket（待建）
  audio.py            PortAudio 收音 + 原音落地（已建立）
  config.py           設定集中處（已建立）
  test_audio.py       裝置解析與設定載入自檢（已建立）
  asr.py              sherpa-onnx：VAD + Whisper（待建）
  diarize.py          語者嵌入 + 聚類（線上 / 全域）+ 主導語言統計（待建）
  translate.py        Claude API + 術語表注入 + 多目標語言（待建）
  refine.py           併入下一句請求的二次修飾（待建）
  keyproxy.py         金鑰輪替與配額（待建）
  store.py            SQLite（待建）
  postprocess.py      會後重跑與逐字稿輸出（待建）
models/               sherpa-onnx 模型（whisper, silero-vad, speaker-embedding）
recordings/           錄音落地位置（git 忽略）
config.json           執行期設定（git 忽略，由 /api/config 寫入）
start.bat             Windows 啟動腳本（含 kill 舊程序、自動建 venv 與 build）
start.command         macOS 啟動腳本
```

每檔控制在 300 行內。

## 分批執行

**第零批（已完成）** — dashboard 骨架 + 四個頁面
從 OpenWA-lab 整包複製後裁去 WhatsApp 專屬部分，`npm run build` 通過，Login 頁已驗證渲染。四個頁面尚未接後端，需要 API 才會顯示資料。

**第一批（已完成，待實機驗收）** — FastAPI 骨架 + 音訊擷取
`server/` 建立完成：`config.py`、`audio.py`、`main.py`、`test_audio.py`，兩平台啟動腳本。

已驗證（Windows）：
- 7 項自檢通過
- 裝置列舉與中文名稱比對正常
- 端到端擷取——播放測試音經「立體聲混音」錄回，peak 0.0396、0 掉塊、16 kHz 單聲道 PCM_16
- FastAPI 同時服務 dashboard 靜態檔與 API，`/api/*` 未知路徑回 404 而非 HTML

尚未驗證：實際 Teams 會議 + 虛擬音效裝置的組合，以及 macOS。這兩項需要實機，是本批真正的驗收條件。

`/api/recording/status` 回傳的 `peak` 就是驗收工具：接上 Teams 後若持續為 0，代表音訊沒進來（多半是輸出裝置設定錯或用了系統靜音）。

**第二批（已完成）** — sherpa-onnx 管線
`asr.py`（VAD + Whisper + 簡轉繁 + 崩塌回退 + 長片段切塊）、`diarize.py`（語者嵌入 + 線上/離線聚類 + 主導語言）。

實測（Windows，tiny int8，2 執行緒）：realtime factor 0.06；語者分離同一人相似度 0.80/0.91、不同人 0.10-0.16，門檻 0.55 落在中間空隙，線上與離線分群皆正確。

**第三批（已完成）** — 翻譯 + 字幕頁
`translate.py`（Claude API、術語表注入、多目標語言、修飾併入下一句請求）、`pipeline.py`、`hub.py`、`/live` 字幕頁 + WebSocket 新增/更新事件。

**第四批（已完成）** — 設定 + 儲存 + 會後模式
`store.py`（SQLite）、`llm.py`（供應商設定 + 金鑰輪替）、`postprocess.py`（大模型重跑 + 全域聚類 + markdown 輸出）、`Capture` / `Sessions` / `Glossary` / `Display` 四個頁面接上 REST API。

## 驗證狀態

已在 Windows 實測通過：

| 項目 | 結果 |
|---|---|
| 自動測試 | 8 + 13 + 11 = 32 項通過（單元、管線、端到端） |
| 端到端擷取 | 播放測試音經回錄裝置錄回，44.1 kHz 雙聲道轉 16 kHz 單聲道，0 掉塊 |
| 完整鏈路 | 擷取 → VAD → 語者分離 → 辨識 → WebSocket → 字幕頁顯示，含發言者標示 |
| small 模型效能 | RTF 0.20（4 執行緒）/ 0.23（2 執行緒），20 邏輯核心。realtime factor 風險對此類硬體已解除 |
| 實測延遲 | 16 秒長句於語畢後 4.8 秒上字幕（含模型首次載入 1.2 秒）。一般 3-5 秒短句約 2 秒 |
| 會後輸出 | markdown 逐字稿含時間戳與發言者分段 |
| 四個頁面 | 收音、會議紀錄、詞彙表、字幕顯示，皆實際渲染確認 |

模型：`tiny` 與 `small` 已下載於 `models/`（約 1.5 GB）。即時預設 `small` 的 int8 權重；會後處理改用同一模型的 float32 權重與全部核心，因為那時只在乎準確度。`models/` 不進版控，新機器要另外下載。

尚未驗證，需要實機：

- 實際 Teams 會議 + 虛擬音效裝置（VB-Cable / BlackHole）
- macOS 整體
- 越南語與台灣國語的辨識率——這是目前最大的未知，手上只有英語測試音
- 真實 Claude API 翻譯（測試用替身，未消耗額度）
- 電視觀看距離下的字幕可讀性

## 風險

| 風險 | 說明 | 應對 |
|---|---|---|
| 越南語辨識率 | CPU `small` 模型下可能不足，這是本案最大技術風險 | 仍未驗證——手上沒有越南語與台灣國語測試音，需實機會議確認；不足則該語言改走雲端 ASR |
| ~~CPU realtime factor~~ | ~~超過 1.0 會累積延遲直到崩潰~~ | 已解除：small 實測 RTF 0.20，餘裕充足。管線仍保留積壓監測作為異常機器的保險 |
| 三語言偵測 | 三個候選比兩個容易錯，首次遇到新語者的那幾句風險最高 | 偵測候選限縮在設定的語言集合內；主導語言確立後即穩定；仍不足則允許在設定中手動指定某語者的語言 |
| 中英夾雜誤判 | 台灣國語常夾英文詞，可能被判定為該語者改講英語，主導語言被翻掉 | 中↔英這組的切換門檻明顯提高；允許把語者語言釘死；術語表可標記常見夾雜詞保留原文 |
| 簡體輸出 | Whisper 中文一律輸出簡體，直接上電視非常顯眼 | ASR 後接 OpenCC `s2twp`；翻譯提示詞明確指定台灣繁體 |
| 台灣國語口音 | `small` 模型下辨識率再降，含台語借詞 | 術語表組成 initial prompt 偏置詞彙；會後 `large-v3` 重跑；第二批實測 |
| 虛擬音效裝置部署 | 兩個平台都要額外安裝驅動，macOS 還需授權，非技術人員可能卡住 | 寫成圖文安裝說明；程式啟動時檢查裝置是否存在並給明確錯誤訊息 |
| 跨平台差異 | 只在單一平台開發容易漏掉另一平台問題 | 每批驗收 Windows 與 Mac 各跑一次，不累積到最後才測 |
| 聚類準確度 | 單通道下語者分離同時決定「誰在講」與「翻譯方向」，錯了會連帶翻錯方向 | 主導語言需連續數句衝突才切換；會後全域聚類可修正；仍不足則換 pyannote（需處理 HF gated model 授權） |
| loopback 靜音 | 播放裝置靜音時 loopback 擷取到無聲，「不外放」的做法會直接讓整套失效 | 改用未接喇叭的輸出裝置或虛擬音效裝置；第一批第一件事就驗證 |
| 修飾造成閃爍 | 字幕改寫過於頻繁會干擾閱讀 | 每行只修飾一次即鎖定，不反覆變動 |
| 搶話重疊 | 兩人同時講話時 VAD 切出的片段含兩個人的聲音，聲紋聚類會混亂，連帶語言判錯 | 嵌入相似度低於門檻時標為「不確定」，該句改用自動語言偵測而非主導語言；多人會議 overlap 常見，第二批必須實測 |
| Teams 二次壓縮 | 中文方音訊經 Teams 編碼與降噪後才進來，辨識率低於原始收音 | 第二批分語言各別評估辨識率，不只看整體平均 |

## 刻意不做

| 項目 | 何時該加 |
|---|---|
| 即時串流翻譯（逐字吐） | 整句翻譯品質明顯較好。使用者抱怨延遲時再考慮 |
| 本地 LLM 摘要 | 中越品質不足。有離網需求時再評估 |
| 跨會議語者庫 | 每場會議獨立聚類已夠用。需要「同一個人跨會議識別」時再做 |
| 讀取 Teams UI 取得發言者姓名 | 可省掉手動對應名字，但綁死 Teams 且隨改版失效。手動對應嫌煩時再評估 |
| 登入 / API 金鑰 / 角色權限 | 單機應用不需要，這是從 OpenWA-lab 帶來的多租戶殘留。見下方待確認事項 |
| 桌面應用外殼 | 批次檔啟動已夠用。真的想要雙擊圖示時再包 pywebview，架構不用動 |
| 字幕蓋在 Teams 上 | 已確認不需要。若之後想要，得換回 Tauri 或用 pywebview 開置頂視窗 |
| 行動端 | 桌面先跑起來再說 |

## 待確認事項

**1. 遠端與會者需要看到字幕嗎？**

電視解決了會議室內的觀看需求，室內不同母語的人各自讀自己那一行。但遠端與會者看不到電視。

若他們也需要，最省的做法是字幕頁開放區域網路或內網存取，遠端用瀏覽器開同一個網址。這牽涉到這台電腦要能被外部連到（防火牆、VPN），不是純程式問題。

目前先當作不需要，只服務會議室現場。

目前無其他待確認事項。
